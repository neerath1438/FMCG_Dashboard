# HTTP Server - Redirect to HTTPS (when SSL is ready)
server {
    listen 80;
    server_name retail.wersel.co.uk www.retail.wersel.co.uk;
    client_max_body_size 100M;
    
    # For now, serve the app directly
    # Later: uncomment the line below to redirect to HTTPS
    # return 301 https://$server_name$request_uri;
    
    root /usr/share/nginx/html;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json;

    # Handle React Router
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}

# HTTPS Server (uncomment when SSL certificate is ready)
# server {
#     listen 443 ssl http2;
#     server_name retail.wersel.co.uk www.retail.wersel.co.uk;
#     client_max_body_size 100M;
#     
#     # SSL Certificate paths (update after running certbot)
#     ssl_certificate /etc/letsencrypt/live/retail.wersel.co.uk/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/retail.wersel.co.uk/privkey.pem;
#     
#     # SSL Configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     
#     root /usr/share/nginx/html;
#     index index.html;
#
#     # Gzip compression
#     gzip on;
#     gzip_vary on;
#     gzip_min_length 1024;
#     gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json;
#
#     # Handle React Router
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     # Cache static assets
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#     }
# }
