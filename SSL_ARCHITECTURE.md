# FMCG Dashboard SSL Architecture

## Current Architecture (After SSL Setup)

```
┌─────────────────────────────────────────────────────────────────┐
│                         INTERNET                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTPS (443)
                              │ HTTP (80) → Redirect to HTTPS
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    SERVER: 20.0.161.242                          │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    NGINX (Reverse Proxy)                    │ │
│  │                                                              │ │
│  │  ┌──────────────────────┐  ┌──────────────────────┐        │ │
│  │  │ retail.wersel.co.uk  │  │retail-api.wersel.co.uk│       │ │
│  │  │   (Frontend HTTPS)   │  │   (Backend HTTPS)     │       │ │
│  │  │   Port 443           │  │   Port 443            │       │ │
│  │  │                      │  │                       │       │ │
│  │  │  SSL Certificate:    │  │  SSL Certificate:     │       │ │
│  │  │  Let's Encrypt       │  │  Let's Encrypt        │       │ │
│  │  └──────────┬───────────┘  └──────────┬────────────┘       │ │
│  │             │                          │                     │ │
│  │             │ Proxy to                 │ Proxy to            │ │
│  │             │ localhost:3001           │ localhost:8080      │ │
│  │             ▼                          ▼                     │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │              DOCKER CONTAINERS                               ││
│  │                                                               ││
│  │  ┌──────────────────┐  ┌──────────────────┐  ┌────────────┐││
│  │  │  fmcg_frontend   │  │  fmcg_backend    │  │fmcg_mongodb│││
│  │  │                  │  │                  │  │            │││
│  │  │  React + Vite    │  │  FastAPI         │  │  MongoDB   │││
│  │  │  Port: 3001:80   │  │  Port: 8080:8080 │  │  Port:     │││
│  │  │                  │  │                  │  │  27017     │││
│  │  │  Serves:         │◄─┤  API Endpoints   │◄─┤  Database  │││
│  │  │  - Dashboard     │  │  - /upload       │  │            │││
│  │  │  - Analytics     │  │  - /products     │  │            │││
│  │  │  - Reports       │  │  - /chatbot      │  │            │││
│  │  │                  │  │  - /health       │  │            │││
│  │  └──────────────────┘  └──────────────────┘  └────────────┘││
│  │                                                               ││
│  │  Network: fmcg_network (Docker Bridge)                       ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │              SSL CERTIFICATES                                ││
│  │  /etc/letsencrypt/live/retail.wersel.co.uk/                 ││
│  │  /etc/letsencrypt/live/retail-api.wersel.co.uk/             ││
│  └─────────────────────────────────────────────────────────────┘│
└───────────────────────────────────────────────────────────────────┘
```

---

## Request Flow

### Frontend Request Flow
```
User Browser
    │
    │ 1. Request: https://retail.wersel.co.uk
    ▼
Nginx (Port 443)
    │
    │ 2. SSL Termination (decrypt HTTPS)
    │ 3. Verify SSL certificate
    ▼
Nginx Reverse Proxy
    │
    │ 4. Proxy to localhost:3001
    ▼
Docker Container: fmcg_frontend (Port 3001)
    │
    │ 5. Serve React application
    ▼
User Browser
    │
    │ 6. Render Dashboard
    └─► User sees the application
```

### Backend API Request Flow
```
Frontend (React App)
    │
    │ 1. API Call: https://retail-api.wersel.co.uk/products
    ▼
Nginx (Port 443)
    │
    │ 2. SSL Termination (decrypt HTTPS)
    │ 3. Verify SSL certificate
    ▼
Nginx Reverse Proxy
    │
    │ 4. Proxy to localhost:8080
    ▼
Docker Container: fmcg_backend (Port 8080)
    │
    │ 5. FastAPI processes request
    ▼
MongoDB Container (Port 27017)
    │
    │ 6. Query database
    ▼
Backend Container
    │
    │ 7. Return JSON response
    ▼
Nginx
    │
    │ 8. Encrypt response (HTTPS)
    ▼
Frontend (React App)
    │
    │ 9. Display data in UI
    └─► User sees the results
```

---

## SSL Certificate Flow

```
┌──────────────────────────────────────────────────────────────┐
│                    Let's Encrypt CA                           │
│              (Certificate Authority)                          │
└──────────────────────┬───────────────────────────────────────┘
                       │
                       │ 1. Request Certificate
                       │    (via Certbot)
                       │
                       ▼
┌──────────────────────────────────────────────────────────────┐
│                  Your Server (20.0.161.242)                   │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐│
│  │                     Certbot                               ││
│  │                                                            ││
│  │  2. Verify domain ownership                               ││
│  │     - HTTP challenge (port 80)                            ││
│  │     - DNS challenge (TXT record)                          ││
│  └──────────────────────────────────────────────────────────┘│
│                       │                                        │
│                       │ 3. Receive Certificate                │
│                       ▼                                        │
│  ┌──────────────────────────────────────────────────────────┐│
│  │        /etc/letsencrypt/live/retail.wersel.co.uk/        ││
│  │        /etc/letsencrypt/live/retail-api.wersel.co.uk/    ││
│  │                                                            ││
│  │  - fullchain.pem (public certificate)                     ││
│  │  - privkey.pem (private key)                              ││
│  │  - cert.pem (certificate only)                            ││
│  │  - chain.pem (certificate chain)                          ││
│  └──────────────────────────────────────────────────────────┘│
│                       │                                        │
│                       │ 4. Nginx reads certificates           │
│                       ▼                                        │
│  ┌──────────────────────────────────────────────────────────┐│
│  │                     Nginx                                 ││
│  │                                                            ││
│  │  ssl_certificate /etc/letsencrypt/live/.../fullchain.pem ││
│  │  ssl_certificate_key /etc/letsencrypt/live/.../privkey.pem││
│  └──────────────────────────────────────────────────────────┘│
└────────────────────────────────────────────────────────────────┘
                       │
                       │ 5. Serve HTTPS traffic
                       ▼
                  User Browser
                  (Secure Connection 🔒)
```

---

## Auto-Renewal Process

```
┌────────────────────────────────────────────────────────────┐
│              Certbot Timer (systemd)                        │
│         Runs twice daily: 00:00 and 12:00                   │
└─────────────────────┬──────────────────────────────────────┘
                      │
                      │ Check certificate expiry
                      ▼
              ┌───────────────┐
              │ Days to expiry│
              │   > 30 days?  │
              └───────┬───────┘
                      │
        ┌─────────────┴─────────────┐
        │                           │
        ▼ YES                       ▼ NO
┌───────────────┐          ┌────────────────┐
│ Do nothing    │          │ Renew certificate│
│ (skip renewal)│          │ (auto-renewal)  │
└───────────────┘          └────────┬────────┘
                                    │
                                    │ Renewal successful?
                                    ▼
                           ┌────────────────┐
                           │ Reload Nginx   │
                           │ (new cert)     │
                           └────────────────┘
                                    │
                                    ▼
                           ┌────────────────┐
                           │ Send email     │
                           │ notification   │
                           └────────────────┘
```

---

## Port Mapping Summary

| Service          | External Port | Internal Port | Protocol | Access        |
|------------------|---------------|---------------|----------|---------------|
| Nginx HTTP       | 80            | 80            | HTTP     | Public        |
| Nginx HTTPS      | 443           | 443           | HTTPS    | Public        |
| Frontend         | 3001          | 80            | HTTP     | Localhost     |
| Backend          | 8080          | 8080          | HTTP     | Localhost     |
| MongoDB          | 27017         | 27017         | TCP      | Docker Network|

---

## Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│                    Security Layers                           │
└─────────────────────────────────────────────────────────────┘

Layer 1: Firewall (UFW)
    ├─ Allow: Port 80 (HTTP)
    ├─ Allow: Port 443 (HTTPS)
    ├─ Allow: Port 22 (SSH)
    └─ Deny: All other ports

Layer 2: Nginx
    ├─ SSL/TLS Encryption (TLS 1.2, 1.3)
    ├─ Strong Cipher Suites
    ├─ HTTP → HTTPS Redirect
    └─ Reverse Proxy (hides internal ports)

Layer 3: Docker Network Isolation
    ├─ fmcg_network (bridge)
    ├─ Containers communicate internally
    └─ No direct external access to containers

Layer 4: Application Security
    ├─ CORS Configuration
    ├─ API Authentication (if implemented)
    └─ Input Validation

Layer 5: Database Security
    ├─ MongoDB not exposed externally
    ├─ Only accessible via Docker network
    └─ Authentication (if configured)
```

---

## DNS Configuration

```
┌─────────────────────────────────────────────────────────────┐
│              DNS Provider (e.g., Cloudflare)                 │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ DNS Resolution
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    A Records                                 │
│                                                               │
│  retail.wersel.co.uk          → 20.0.161.242                │
│  www.retail.wersel.co.uk      → 20.0.161.242                │
│  retail-api.wersel.co.uk      → 20.0.161.242                │
│  www.retail-api.wersel.co.uk  → 20.0.161.242                │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ User requests domain
                              ▼
┌─────────────────────────────────────────────────────────────┐
│              User's Browser/Device                           │
│                                                               │
│  1. Query DNS for retail.wersel.co.uk                       │
│  2. Receive IP: 20.0.161.242                                │
│  3. Connect to 20.0.161.242:443 (HTTPS)                     │
│  4. Nginx serves the application                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Monitoring Points

```
┌─────────────────────────────────────────────────────────────┐
│                    Monitoring Checklist                      │
└─────────────────────────────────────────────────────────────┘

1. Nginx Logs
   ├─ Access: /var/log/nginx/access.log
   └─ Error: /var/log/nginx/error.log

2. Docker Container Logs
   ├─ Frontend: docker logs fmcg_frontend
   ├─ Backend: docker logs fmcg_backend
   └─ MongoDB: docker logs fmcg_mongodb

3. SSL Certificate Status
   ├─ Expiry: sudo certbot certificates
   └─ Renewal: sudo systemctl status certbot.timer

4. System Resources
   ├─ CPU: top or htop
   ├─ Memory: free -h
   └─ Disk: df -h

5. Application Health
   ├─ Frontend: https://retail.wersel.co.uk
   ├─ Backend: https://retail-api.wersel.co.uk/health
   └─ Database: docker exec fmcg_mongodb mongosh
```

---

## Backup Strategy

```
┌─────────────────────────────────────────────────────────────┐
│                    Backup Components                         │
└─────────────────────────────────────────────────────────────┘

1. SSL Certificates
   Location: /etc/letsencrypt/
   Backup: tar -czf letsencrypt-backup.tar.gz /etc/letsencrypt/

2. Nginx Configuration
   Location: /etc/nginx/sites-available/fmcg-retail.conf
   Backup: cp /etc/nginx/sites-available/fmcg-retail.conf ~/backup/

3. MongoDB Data
   Location: Docker volume (mongodb_data)
   Backup: docker exec fmcg_mongodb mongodump --out /backup

4. Application Code
   Location: ~/fmcg-server/FMCG_Dashboard
   Backup: Git repository (already version controlled)

5. Environment Variables
   Location: .env files
   Backup: Securely store in password manager
```

---

## Troubleshooting Decision Tree

```
                    ┌─────────────────┐
                    │  Issue Occurs   │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │ Can access site?│
                    └────────┬────────┘
                             │
                ┌────────────┴────────────┐
                │                         │
                ▼ NO                      ▼ YES
        ┌───────────────┐         ┌──────────────┐
        │ Check DNS     │         │ SSL Error?   │
        │ nslookup      │         └──────┬───────┘
        └───────┬───────┘                │
                │              ┌─────────┴─────────┐
                ▼              │                   │
        ┌───────────────┐      ▼ YES              ▼ NO
        │ DNS OK?       │  ┌────────────┐   ┌──────────────┐
        └───────┬───────┘  │Check certs │   │ CORS Error?  │
                │          │certbot     │   └──────┬───────┘
        ┌───────┴───────┐  └────────────┘          │
        │               │                  ┌────────┴────────┐
        ▼ YES           ▼ NO               │                 │
┌───────────────┐  ┌────────────┐         ▼ YES             ▼ NO
│Check Nginx    │  │Update DNS  │   ┌──────────────┐  ┌──────────┐
│systemctl      │  │Wait 30 min │   │Check CORS    │  │Check app │
└───────────────┘  └────────────┘   │docker-compose│  │logs      │
                                     └──────────────┘  └──────────┘
```

---

**Architecture Version**: 1.0  
**Last Updated**: 2026-01-14  
**Server**: 20.0.161.242
